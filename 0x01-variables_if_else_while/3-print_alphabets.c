#include <stdio.h>

/**
 * main - Entry point
 * Description: print both uppercase and lowercase aplhabets
 * Author: KingVik-Planet
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char kp;
	char kgp;

	for (kp = 'a'; kp <= 'z'; kp++)
		putchar(c);
	for (kgp = 'A'; kgp <= 'Z'; kgp++)
		putchar(kgp);
	putchar('\n');
	return (0);
}
