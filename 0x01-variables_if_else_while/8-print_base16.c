#include <stdio.h>

/**
 * main - Entry point
 * Description: program that prints all the numbers of base 16 in lowercase
 * autor: KingVik-Planet
 * Date: 17th March 2023
 * Return: Always 0 (Success)
 */
int main(void)
{
	int kingsley;
	char chika;

	for (kingsley = 48; kingsley < 58; kingsley++)
		putchar(sammy);
	for (chika = 'a'; chika <= 'f'; chika++)
		putchar(chika);
	putchar('\n');
	return (0);
}
