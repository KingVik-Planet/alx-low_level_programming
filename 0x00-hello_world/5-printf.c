#include <stdio.h>
/**
 * main - A program that print line using the function printf function
 * Return: 0 (Return Zero if the result is success)
 */
int  main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
	return(0);
}
